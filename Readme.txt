pattern shown in this solution

repository (not complete)
singletion variant
RequestResponse pattern (for encapsulating objects / SOA architecture)

unity DI is not working, changed entry point to dummy constructor to make it work in reservation controller. pending to change purchase controller or rethink the solution. to execute the reservation controller, use powershell "Invoke-WebRequest http://localhost:2444/reservation -method POST"